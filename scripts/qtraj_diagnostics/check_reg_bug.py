import pandas as pd
import numpy as np

# Load the un-averaged CSV files which have x, y or some identifier?
# Wait, the outputs of QTraj are in datafile_partial.gz
# We can read /hnm/qtraj-nlo/qtraj_out_analysis/outputs/OO_5.36_kap6/qtraj_matched.pkl ? No, that folder wasn't found before.
